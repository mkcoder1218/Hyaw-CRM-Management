import {prisma} from "../db";
import {dedupeFormattedLeads,formatSearchCandidate} from "../services/lead-formatter";
import {searchPublicLeads} from "../services/lead-search";
import {buildCallingStrategy} from "../services/calling-expert";
let running=false;
export async function runLeadDiscovery(campaignId?:string){if(running&&!campaignId)return {processed:0,skipped:true};running=true;let processed=0;try{const campaigns=await prisma.leadCampaign.findMany({where:{active:true,...(campaignId?{id:campaignId}:{})}});for(const campaign of campaigns){const q=[campaign.query,campaign.industry,campaign.location].filter(Boolean).join(" ");const candidates=dedupeFormattedLeads((await searchPublicLeads(q)).map(x=>formatSearchCandidate(x,campaign)).filter((x):x is NonNullable<typeof x>=>Boolean(x)));for(const lead of candidates){const exists=await prisma.discoveredLead.findUnique({where:{tenantId_sourceUrl:{tenantId:campaign.tenantId,sourceUrl:lead.sourceUrl}}});if(exists)continue;const strategy=await buildCallingStrategy(lead,campaign);await prisma.discoveredLead.create({data:{tenantId:campaign.tenantId,campaignId:campaign.id,name:lead.name,company:lead.company,website:lead.website,location:lead.location,industry:lead.industry,sourceUrl:lead.sourceUrl,sourceName:lead.sourceName,description:lead.description,rawData:lead.rawData as any,fitScore:strategy.fitScore,priority:strategy.priority,likelyProblems:strategy.likelyProblems,strategy:strategy.strategy,openingLine:strategy.openingLine,firstCallGoal:strategy.firstCallGoal,objections:strategy.objections as any,nextAction:strategy.nextAction}});processed++}}return {processed,skipped:false}}finally{running=false}}
export function startLeadFetcherWorker(){const minutes=Math.max(15,Number(process.env.LEAD_DISCOVERY_INTERVAL_MINUTES)||60);setInterval(()=>{void runLeadDiscovery().catch(e=>console.error("Lead discovery worker failed",e))},minutes*60_000);console.log(`Lead discovery worker scheduled every ${minutes} minutes`)}

import type {SearchCandidate} from "./lead-formatter";import {getPlatformValue} from "./platform-ai-settings";
export async function searchPublicLeads(query:string):Promise<SearchCandidate[]>{const key=await getPlatformValue("leadSearchApiKey");if(!key)throw new Error("SERPER_API_KEY is not configured");const r=await fetch("https://google.serper.dev/search",{method:"POST",headers:{"X-API-KEY":key,"Content-Type":"application/json"},body:JSON.stringify({q:query,num:20})});if(!r.ok)throw new Error(`Lead search failed: ${r.status}`);const body:any=await r.json();return (body.organic||[]).map((x:any)=>({title:String(x.title||""),link:String(x.link||""),snippet:x.snippet?String(x.snippet):undefined,source:"web-search"})).filter((x:SearchCandidate)=>x.title&&x.link)}

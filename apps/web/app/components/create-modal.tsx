"use client";import {useState,type FormEvent} from "react";import {Button} from "../../components/ui/button";import {Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle,DialogTrigger} from "../../components/ui/dialog";import {Input} from "../../components/ui/input";import {Label} from "../../components/ui/label";import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "../../components/ui/select";import {Textarea} from "../../components/ui/textarea";export type CreateField={name:string;label:string;type?:"text"|"email"|"tel"|"number"|"date"|"textarea"|"select";placeholder?:string;options?:(string|{value:string;label:string})[];required?:boolean};export function CreateModal({entity,fields,buttonLabel,onCreated}:{entity:string;fields:CreateField[];buttonLabel?:string;onCreated?:(data:Record<string,string>)=>void|Promise<void>}){const[open,setOpen]=useState(false),[saved,setSaved]=useState(false),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const data=Object.fromEntries(new FormData(e.currentTarget).entries()) as Record<string,string>;try{await onCreated?.(data);setSaved(true);setTimeout(()=>{setOpen(false);setSaved(false)},450)}catch(err){setError(err instanceof Error?err.message:"Could not create record")}}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button>+ {buttonLabel??`New ${entity}`}</Button></DialogTrigger><DialogContent><DialogHeader><p className="eyebrow">Create</p><DialogTitle>New {entity}</DialogTitle><DialogDescription>Add the details below. You can update them later.</DialogDescription></DialogHeader><form onSubmit={submit}><div className="formGrid">{fields.map(f=><div className={f.type==="textarea"?"fullField":""} key={f.name}><Label htmlFor={f.name}>{f.label}{f.required&&" *"}</Label>{f.type==="textarea"?<Textarea id={f.name} name={f.name} placeholder={f.placeholder} required={f.required}/>:f.type==="select"?<Select name={f.name} required={f.required}><SelectTrigger><SelectValue placeholder={`Select ${f.label.toLowerCase()}`}/></SelectTrigger><SelectContent>{f.options?.map(o=>{const v=typeof o==="string"?o:o.value,l=typeof o==="string"?o:o.label;return <SelectItem value={v} key={v}>{l}</SelectItem>})}</SelectContent></Select>:<Input id={f.name} name={f.name} type={f.type??"text"} placeholder={f.placeholder} required={f.required}/>}</div>)}</div>{error?<p className="dataError">{error}</p>:null}<DialogFooter><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit">{saved?"Created ✓":`Create ${entity}`}</Button></DialogFooter></form></DialogContent></Dialog>}
"use client";import {useState,type FormEvent} from "react";import {Button} from "../../components/ui/button";import {Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle,DialogTrigger} from "../../components/ui/dialog";import {Input} from "../../components/ui/input";import {Label} from "../../components/ui/label";import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "../../components/ui/select";import {Textarea} from "../../components/ui/textarea";export type CreateField={name:string;label:string;type?:"text"|"email"|"tel"|"number"|"date"|"textarea"|"select";placeholder?:string;options?:string[];required?:boolean};export function CreateModal({entity,fields,buttonLabel,onCreated}:{entity:string;fields:CreateField[];buttonLabel?:string;onCreated?:(data:Record<string,string>)=>void}){const[open,setOpen]=useState(false),[saved,setSaved]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget).entries()) as Record<string,string>;onCreated?.(data);setSaved(true);setTimeout(()=>{setOpen(false);setSaved(false)},450)}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button>+ {buttonLabel??`New ${entity}`}</Button></DialogTrigger><DialogContent><DialogHeader><p className="eyebrow">Create</p><DialogTitle>New {entity}</DialogTitle><DialogDescription>Add the details below. You can update them later.</DialogDescription></DialogHeader><form onSubmit={submit}><div className="formGrid">{fields.map(f=><div className={f.type==="textarea"?"fullField":""} key={f.name}><Label htmlFor={f.name}>{f.label}{f.required&&" *"}</Label>{f.type==="textarea"?<Textarea id={f.name} name={f.name} placeholder={f.placeholder} required={f.required}/>:f.type==="select"?<Select name={f.name} required={f.required}><SelectTrigger><SelectValue placeholder={`Select ${f.label.toLowerCase()}`}/></SelectTrigger><SelectContent>{f.options?.map(o=><SelectItem value={o} key={o}>{o}</SelectItem>)}</SelectContent></Select>:<Input id={f.name} name={f.name} type={f.type??"text"} placeholder={f.placeholder} required={f.required}/>}</div>)}</div><DialogFooter><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit">{saved?"Created ✓":`Create ${entity}`}</Button></DialogFooter></form></DialogContent></Dialog>}